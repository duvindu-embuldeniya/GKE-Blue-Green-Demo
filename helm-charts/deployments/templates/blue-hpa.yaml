apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: django-blue-hpa
  namespace: {{ .Values.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: django-blue
  minReplicas: {{ .Values.blue.hpa.minReplicas }}
  maxReplicas: {{ .Values.blue.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.blue.hpa.cpuUtilization }}